import React from 'react';

const MainView = ({ className }) => {
  return (
    <div className={className}>
      <h1>This is a React View</h1>
    </div>
  );
};

MainView.propTypes = {};

export default MainView;
